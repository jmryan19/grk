from argparse import ArgumentParser
class Config():
    def __init__(self, wandb_offline = False,
                 prime = 67,
                 data_budget = 1000,
                 train_budget = 5000,
                 num_layers = 2,
                 dim_model = 256,
                 num_heads = 4,
                 batch_size = 64,
                 learning_rate = 1e-4,
                 weight_decay = 1,
                 model = 'transformer',
                 fcn_hidden_width = 512,
                 loss = 'cross_entropy',
                 kernel_bandwidth = 1.0,
                 iter = 0,
                 max_epochs = 300,
                 training_fraction = 0.5,
                 base = False,
                 openai = True,
                 num_steps = 1e5,
                 max_train_val = 67,
                 plot_inner_products = False,
                 data = 'cifar10',
                 experiment = 'nc_original',
                 save_dist_class_means = True,
                 custom_cifar = False,
                 repeat = False,
                 kind_custom = 'top',
                 seed = None,
                 sanity = False):
        self.wandb_offline = wandb_offline
        self.prime = prime
        self.data_budget = data_budget
        self.train_budget = train_budget
        self.num_layers = num_layers
        self.dim_model = dim_model
        self.num_heads = num_heads
        self.batch_size = batch_size
        self.learning_rate = learning_rate
        self.weight_decay = weight_decay
        self.model = model
        self.fcn_hidden_width = fcn_hidden_width
        self.loss = loss 
        self.kernel_bandwidth = kernel_bandwidth
        self.iter = iter
        self.max_epochs = max_epochs
        self.training_fraction = training_fraction
        self.base = base
        self.openai = openai
        self.num_steps = num_steps
        self.max_train_val = max_train_val
        self.plot_inner_products = plot_inner_products
        self.data = data
        self.experiment = experiment
        self.train_data_len = 50_000
        self.validation_data_len = 10_000
        self.save_dist_class_means = save_dist_class_means
        self.custom_cifar = custom_cifar
        self.repeat = repeat
        self.kind_custom = kind_custom
        self.seed = seed
        self.sanity = sanity
